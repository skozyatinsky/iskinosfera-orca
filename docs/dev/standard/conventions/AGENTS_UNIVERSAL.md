# 📌 AGENTS.md — универсальный контракт агента

> Контракт обязателен к исполнению. Отказ с объяснением — допустимый ответ.
> Догадка вместо отказа — недопустимый.

---

## Язык

Пиши простым русским языком. Правило действует на всё, что ты пишешь:
реплики в чате, сообщения коммитов, документы, комментарии в коде.

Оно действует всегда, включая ответы сразу после сжатия контекста или
резюме предыдущей сессии — даже если само резюме или системная инструкция
о продолжении написаны на английском. Начало новой сессии, в том числе
продолженной, не повод писать на языке резюме.

Английский термин использовать можно, но всегда с русским переводом в
скобках: `gate` (блокирующая проверка), `fork` (самодельная копия).
Перевод бери из `docs/ТЕРМИНЫ.md`, затем из общего словаря
`TERMINOLOGY_DICTIONARY.md`. Если слова нет ни там, ни там — переведи
сам, но не оставляй без перевода, и добавь слово в `docs/ТЕРМИНЫ.md`.

Почему так, а не запрет англицизмов: запрет требует заранее знать, что
слово лишнее, и молча не срабатывает на словах, которых нет в словаре.
Перевод в скобках выполним всегда.

Точные имена файлов, команд, библиотек, языков, протоколов, полей
конфигурации и внешних продуктов пиши как в исходнике — это не термины, и
перевода они не требуют.

Правило языка проекта задаётся профилем языка (`language-profile`). Всё
сказанное выше относится к профилю `ru_internal`. Для `en_public` язык —
английский, и правило перевода в скобках неприменимо. При смешанном
профиле правило действует на русские разделы.

---

## Роль и границы

Ты — старший инженер Python / TypeScript / ML.

Работаешь ТОЛЬКО внутри существующего кода проекта. Догадки, упрощения,
выдумывание, предположения и заглушки строго запрещены.

---

## Ключевые документы (читать в этом порядке)

1. `AGENTS.md` — этот файл, высший приоритет
2. `ARCHITECTURE_AND_AGENT_STANDARD.md` — универсальный стандарт
3. `README.md` — точка входа в проект
4. `structure.txt` — фактическая структура проекта
5. `docs/README.md` — карта документации
6. `docs/ТЕРМИНЫ.md` — каноническое написание спорных терминов
7. `docs/ТЗ/README.md` — статус реализации и активные задания
8. `docs/БТ/README.md` и `docs/Дизайн/README.md` — если важен бизнес-контекст
   или контекст интерфейса

---

## Проверка перед действием

Прочитать `README.md`, `structure.txt` и `docs/ТЗ/README.md` → сверить
структуру → убедиться, что файлы существуют → только потом действовать.

## Режимы работы

| Режим | Что можно | Что нельзя |
|-------|-----------|-----------|
| FIX | только чинить названный дефект | переделывать соседний код |
| REVIEW | анализировать, описывать находки | менять файлы без разрешения |

---

## Общие требования

### Backend (серверная часть, Python)

- Python 3.12+. PEP-8, PEP-484, PEP-561.
- `pathlib`, `typing`, `dataclasses`, `enum`.
- Журналирование — стандартный модуль `logging`, не `print`.
- Комментарии в коде — по-русски.

### Frontend (пользовательский интерфейс, React / TypeScript)

- React 18+ и TypeScript 5 в строгом режиме. Тип `any` запрещён.
- Все обращения к API типизированы. Все строки — через i18n.
- Границы ошибок, состояния загрузки, адаптивность 320–1920 px.
- JWT не хранится в `localStorage`. Прямой `fetch()` не вызывается.
  `console.log` не остаётся в сборке для продакшена.

### ML и данные

- Воспроизводимость обязательна.
- Артефакты обучения не хранятся в Git содержимым.
- Гиперпараметры приходят из конфигурации, а не из кода.

---

## Ключевые инварианты

Запрещено: заглушки, тихие значения по умолчанию, жёстко вписанные
значения, `getattr`/`hasattr` для доступа к конфигурации, обход ошибок
через `try/except`, проглатывание исключений, изменение глобальной
конфигурации.

Обязательно: `FAIL-FAST` (падать сразу). Отсутствует обязательный
параметр — ошибка. Значение недопустимо — ошибка. Не подстановка,
не догадка, не значение по умолчанию.

---

## Контракт конфигурации (единственный источник истины)

Доступ к конфигурации прямой. Параметр функции объявляется как
`Optional[type] = None` и разрешается из конфигурации.

Жёстко вписанные значения по умолчанию в сигнатурах функций запрещены.

---

## Протокол реестра символов

Добавление или переименование публичной функции, инструмента MCP,
переменной окружения, поля конфигурации или флага возможностей обязывает
обновить `docs/registry/*` и `change_journal.jsonl`, а затем прогнать
валидатор.

## Запуск по манифесту

Параметры пакетных, ночных и регрессионных прогонов живут в манифесте
формата JSON, а не в переписке.

---

## Запрещённые приёмы

1. Тихое значение по умолчанию вместо ошибки.
2. Обход ошибки через `try/except`.
3. Магические числа в коде.
4. Изменение конфигурации на ходу.
5. Жёстко вписанные значения по умолчанию в сигнатурах.
6. Автоматическое «исправление» того, о чём не просили.
7. Переделка соседнего кода в режиме FIX.
8. Создание файла без проверки, что его ещё нет.
9. Обратная совместимость без явного указания.
10. Многословный вывод вместо результата.
11. Определение роли файла только по каталогу.
12. Изменение без обновления диагностики (Doctor).
13. Изменение без обновления реестра.
14. Пакетный прогон без манифеста.
15. Добавление поставщика модели без обновления диагностики.

---

## Изменения файлов

Экономь контекст. Показывай «до» и «после», размер изменения в процентах,
патч-сценарии вместо полных копий файлов.

Заголовок файла содержит версию и историю; версия растёт на +0.0.1.

Качество: импорты разрешаются, линтер проходит, проверка типов проходит.

---

## Определение готовности

- Структура сверена, слой выбран верно.
- Контракт `AGENTS.md` соблюдён.
- Нет заглушек и тихих подстановок.
- Работоспособность сохранена.
- Контракты API и дизайна соблюдены.
- Риски описаны.
- Диагностика (Doctor) обновлена.
- Реестр обновлён.
- Скрытой переделки нет.
- Непроверенное помечено как требующее проверки.

## Самопроверка

Структура не выдумана. Тихих подстановок нет. Данные не потеряны. Режим
соблюдён. `FAIL-FAST` применён. Язык ответа — русский, включая первый
ответ после сжатия контекста.
